apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-grpc-deployment
  labels:
    component: metadata-grpc-server
spec:
  template:
    spec:
      containers:
      - name: container
        resources:
          limits:
            memory: 1Gi

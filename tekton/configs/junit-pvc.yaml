apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: junit-pvc
  namespace: tektoncd
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 20Gi
  storageClassName: "standard"

language: python

python:
  - "3.6"

install:
  - pip install -U pip wheel
  - pip install pipenv pylint yapf
  - pipenv install --system

script:
  # run yapf to try to format python code, if any diff then fail
  - no_diff=$(yapf -r -d py) && [[ -z ${no_diff} ]]
  - pylint py/**/*.py

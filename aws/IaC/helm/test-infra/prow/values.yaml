# Default values for prow.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

secret:
  keyValue:
  secretValue:

s3Buckets:
  statusReconciler:
  prowLogs:
  tide:

tokens:
  githubToken:
  hmacToken:

managedGithubOrg:

prowDashboardDomainName:
prowDashboardDomainNameACMCerts: